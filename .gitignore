.env
*.zip
# Overleaf project clone (separate git repo, synced via git.overleaf.com)
/paper/
# raw corpora at repo root, shared by all experiments (experiment_01/data/raw is a committed symlink)
/data/raw/
# large regenerable intermediate (415k gold pairs, ~250MB); rebuild via src/extract_pairs.py
experiment_01/data/processed/kuk_pairs.jsonl
__pycache__/
*.pyc
.venv/
venv/
# LaTeX build artifacts (keep .tex/.bib/.pdf, drop the rest)
*.aux
*.bbl
*.blg
*.fls
*.fdb_latexmk
*.out
*.log
experiment_01/paper/build.log
# scratch (never commit)
/temp/
# grid traces: committed only after the run completes (decision pending on size/compression)
experiment_04/results/runs/
